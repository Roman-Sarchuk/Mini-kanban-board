import PlusIcon from '../../icons/PlusIcon';

function AddColumn() {
    return (
        <button>
            <PlusIcon />
            <span>Add column</span>
        </button>
    )
}

export default AddColumn